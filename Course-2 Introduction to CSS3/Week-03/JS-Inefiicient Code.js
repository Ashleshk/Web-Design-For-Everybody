/*var name = prompt("What is your name?")*/

if (name.length != 0 && name == "Colleen")
      document.write("What a beautiful name");

if (name.length != 0 && name != "Colleen")
      document.write("Hello " +  name);

if (name.length == 0)
   document.write("Feeling shy?");

/*Every conditional is checked instead of just one*/